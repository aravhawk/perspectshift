# Privacy

PerceptShift is local-first.

- Raw frames are not retained by default (`telemetry.include_input_content: false`).
- Reports should avoid embedding personal data from datasets.
- API audit logs record operator actions locally; protect log directories.
- No cloud telemetry is required for core operation.

Operators remain responsible for dataset licensing and privacy compliance for user-supplied data.
